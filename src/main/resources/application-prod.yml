spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://localhost:3306/yuyu?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:user}
    password: ${DB_PASSWORD:user}

workflow:
  security:
    token-secret: ${WORKFLOW_SECURITY_TOKEN_SECRET:prod-token-secret-change-this}
