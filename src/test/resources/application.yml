logging:
  config: classpath:log/logback-spring.xml

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:yuyu;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: sa
    password:
